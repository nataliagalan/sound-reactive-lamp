export const MAX_16_BIT = 65535; // Maximum value for a 16-bit integer
export const BYTE_MASK = 0xff; // Mask to extract lower 8 bits of a 16-bit integer
export const MIN_BRIGHTNESS = 10000; // Minimum brightness level
export const BITS_TO_SHIFT = 8; // Number of bits to shift to isolate the high byte
// In future iterations the frequency range below could be determined via the Max Device UI
export const MINIMUM_FREQUENCY = 80;
export const MID_FREQUENCY = 200;
export const MAXIMUM_FREQUENCY = 500;
